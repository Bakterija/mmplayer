Widget behavior with hovering property that is set to True when mouse is hovering over the widget
and to False when not. Also has on_enter and on_leave methods that get called when hover state changes
