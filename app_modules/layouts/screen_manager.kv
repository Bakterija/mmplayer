#: import VideoFrame app_modules.media_controller.videoframe.VideoFrame

<Jotube_SM>:

    Screen:
        id: media_screen
        name: 'media'
        StackLayout:
            id: media_stack

    Screen:
        id: queue_screen
        name: 'queue'
        StackLayout:
            id: queue_stack

    Screen:
        id: video_screen
        name: 'video'
        VideoFrame:
            id: videoframe

    Screen:
        id: browser_screen
        name: 'browser'
        StackLayout:
            id: browser_stack
